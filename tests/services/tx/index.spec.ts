import TxService from './../../../src/services/tx/index';

describe('Services#tx', () => {
  beforeEach(() => {});
  it('sample', async done => {
    expect(true).toEqual(true);
    done();
  });

});
